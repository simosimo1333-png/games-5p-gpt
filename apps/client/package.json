{
  "name": "@houkago/client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --configLoader runner --host 0.0.0.0",
    "build": "tsc --noEmit && vite build --configLoader runner && node ../../scripts/check-client-bundle.mjs dist",
    "preview": "vite preview --configLoader runner --host 0.0.0.0",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
